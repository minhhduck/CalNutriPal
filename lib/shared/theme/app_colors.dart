import 'package:flutter/material.dart';

/// Application color palette
class AppColors {
  /// Primary brand color
  static const Color primary = Color(0xFF4CAF50);

  /// Secondary brand color
  static const Color secondary = Color(0xFF8BC34A);

  /// Background color for light theme
  static const Color background = Color(0xFFF5F5F5);

  /// Surface color for cards and elevated surfaces
  static const Color surface = Colors.white;

  /// Color for protein values
  static const Color protein = Color(0xFF7B1FA2); // Purple

  /// Color for carbohydrate values
  static const Color carbs = Color(0xFFFF9800); // Orange

  /// Color for fat values
  static const Color fat = Color(0xFF2196F3); // Blue

  /// Success color for positive actions and indicators
  static const Color success = Color(0xFF4CAF50);

  /// Error color for errors and negative actions
  static const Color error = Color(0xFFE53935);

  /// Warning color for warnings and caution indicators
  static const Color warning = Color(0xFFFFC107);

  /// Info color for information and help indicators
  static const Color info = Color(0xFF2196F3);

  /// Disabled color for inactive elements
  static const Color disabled = Color(0xFFBDBDBD);

  /// Text color for dark elements (light theme)
  static const Color textDark = Color(0xFF212121);

  /// Text color for medium emphasis (light theme)
  static const Color textMedium = Color(0xFF757575);

  /// Text color for low emphasis (light theme)
  static const Color textLight = Color(0xFFBDBDBD);

  /// Text color for light elements (dark theme)
  static const Color textDarkTheme = Color(0xFFF5F5F5);
}
